# Roadmap

M0–M11 shipped. Behavior contract: [`SPEC.md`](SPEC.md). Runbook: [`DEPLOY.md`](DEPLOY.md).

Conformance: `bun run check` (typecheck + oxlint + `bun test` / SPEC §18).

No open milestone checklist — new work starts as a SPEC change + tests, not a roadmap essay.
