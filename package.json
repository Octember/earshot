{
  "name": "@bevyl/agent-kit",
  "version": "0.1.0",
  "description": "Shared runtime contract + generic host tools for codex-app-server agents (bunion, tag). Codex/exe.dev only — never the Claude API.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "engines": {
    "bun": ">=1.3"
  },
  "license": "MIT"
}
